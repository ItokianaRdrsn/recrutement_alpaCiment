### Okey creation de model 

# etape 1
    php artisan make:Model Direction -m
        -> le -m permet de faire la migration egalement 
    ensuite aller dans le fichier de migration dans le database/migration 
        -> modifier le schemas de create en rajoutant toutes les colonnes dont on a besoin
